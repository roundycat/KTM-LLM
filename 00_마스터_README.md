# 한의학 그래프 RAG 데이터셋 — 전체 안내

환자의 모호한 호소에서 처방까지 이어지는 한의학 추론을 그래프 + 검색용 본문으로
만든 데이터 모음입니다. 출처: 표준한의학용어집(2006), 한의대 내과학 교과서 처방
데이터(2024), 팔강변증 한의표준임상진료지침(2023), 국가고시 문제집.

## 통합 그래프 (바로 쓰는 핵심)
- `kg_all_nodes.jsonl` — 노드 7,692개
- `kg_all_edges.jsonl` — 엣지 39,647개
- `load_to_neo4j.py` — Neo4j 적재 스크립트

### 노드 종류
| type | 뜻 | 개수 |
|---|---|---|
| 처방 | 한약 처방(구성·출전·계통) | 3,094 |
| 약재 | 구성 약재(귀경·성·미·분류) | 645 |
| 증상 | 처방의 주치(이 중 1,508개는 `is_변증` 표식 = 적응 변증) | 3,873 |
| 변증 | 팔강 8증(음·양·표·리·한·열·허·실) | 8 |
| 증상지표 | 팔강변증 설문 문항(환자 말투에 가까운 표현) | 54 |
| 장부 | 귀경 대상 장기(간·심·비·폐·신 등) | 13 |
| 계통 | 오장 내과 계통 | 5 |

### 엣지 종류
| type | 의미 | 개수 |
|---|---|---|
| 구성 | 처방 → 약재 (용량·단위·수치법) | 25,565 |
| 주치 | 처방 → 증상/변증 (적응증) | 8,509 |
| 지표 | 증상지표 → 변증 (환자말 → 팔강) | 54 |
| 팔강귀속 | 적응 변증 → 팔강 8증 *(auto, 검수필요)* | 1,447 |
| 귀경 | 약재 → 장부 | 972 |
| 계통 | 처방 → 오장 계통 | 3,094 |
| 포함 | 양증⊃표·열·실 / 음증⊃리·한·허 | 6 |

### 추론 경로(이 그래프의 핵심)
환자말(증상지표) →지표→ 팔강변증 ←팔강귀속← 적응변증 ←주치← 처방 →구성→ 약재 →귀경→ 장부

예: "쉽게 피로해진다" → 허증 → 신허(腎虛) → 가미사육탕 → 숙지황·산약·산수유…

## 검색용 본문 (벡터 RAG side)
그래프와 함께 쓰는, 문장형 근거 자료입니다. 임베딩해서 벡터DB에 넣으세요.
- `처방_rag_chunks.jsonl` — 처방별 자기완결 본문 3,094개
- `한의학용어_rag_chunks.jsonl` — 용어 정의 본문 5,878개

## 원천 / 참고 데이터
- `한의학용어_structured.jsonl`, `한의학용어.csv` — 용어집 원천(8,797)
- `처방_kg_nodes/edges.jsonl`, `변증_*.jsonl`, `변증처방_연결_*.jsonl` — 통합 전 단계별 파일

## 적재 & 활용
1. `pip install neo4j` 후 `load_to_neo4j.py` 실행 → 그래프DB 구축.
2. RAG 청크를 임베딩(BGE-m3 등)해 벡터DB(FAISS/Chroma)에 적재.
3. 질의 시: 질문에서 증상 추출 → 그래프에서 관련 부분(증상→변증→처방→약재) 탐색
   + 벡터DB에서 관련 본문 검색 → 둘을 근거로 LLM이 답변(출처 포함).

## 한계 / 다음 단계
- 범위: 내과(內科) 중심. 침구·부인·소아·사상 등은 추가 자료 필요.
- `팔강귀속`(auto)·`is_변증` 판정은 한자 규칙 기반이라 한의사 검수 권장.
- 주치에 변증이 적히지 않은 처방(약 40%)은 질환별 진료지침으로 보강 가능.
- 본 데이터는 의료 의사결정 보조/교육용이며, 최종 판단은 면허 한의사가 해야 합니다.
