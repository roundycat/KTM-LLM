# 한의학 국가시험 LLM 연구 — 전체 통합 인덱스 (논문용)

> `main_1` 브랜치는 본 프로젝트의 **모든 실험·데이터·코드·결과·문서**와, 원본 GraphRAG(`bigse0u1` 브랜치)
> **전부**를 통합한 단일 정리본이다. 아래 인덱스로 모든 상세 내용(방법·수치·출처·재현)을 찾을 수 있다.
> 모든 평가는 **로컬 추론, 비용 $0**(해설 생성만 Claude). 작성 기준일 2026-06-14.

---

## 0. 한눈에 — 무엇을 했나
1. **로컬(한국형) vs 글로벌(범용) 모델 비교 + 용어 RAG + 추론기법(CoT·SC) + 환각완화(vote3)** — 6모델
2. **답안 해설 517 생성 + 2단계 다중에이전트 적대 검증**(정확도 96.9%)
3. **GraphRAG(지식그래프 처방 RAG) + 유형-선택적 RAG** — 6모델 (우리 확장)
4. **bigse0u1 원본 GraphRAG**(Neo4j+Chroma+BGE-m3 seed 벡터화) 통합 + 복제 하니스

---

## 1. 실험별 상세 (방법·결과·문서 위치)

### 1-A. 로컬 vs 글로벌 + RAG + CoT/SC + vote3 (전체 517)
- **방법**: 국시 517문항 5지선다, 정답 분리(누수방지). base / 용어RAG(KIOM 표준용어 주입, 한자우선 매칭) /
  CoT / self-consistency(5회 다수결) / vote3(3회·2합의·기권 환각완화). temp0(base)·0.7(SC/vote).
- **핵심 결과**: Qwen 48.55% > EXAONE 43.13% > Llama 34.24% > SOLAR 33.08%. **CoT+SC가 최대 향상**(Qwen +4.06%p).
  RAG는 한국어능력 비례(EXAONE +2.91%p). vote3는 향상 없음. ⚠️검증셋(51) 노이즈 → 전체 517로 판단.
- **상세 문서**: [`EXPERIMENTS.md`](EXPERIMENTS.md)(전 과정·전 수치), [`HANDOVER.md`](HANDOVER.md)(인수인계·프롬프트/하이퍼파라미터 부록)
- **원자료**: [`results/`](results/) (base/rag/cot/sc5/vote3 × qwen·llama·gemma2·mistral·solar·exaone JSON 24개), `results/report.md`, `results/summary.json`
- **코드**: [`training/`](training/) (evaluate.py·rag.py·config.py·report.py 등), [`scripts/`](scripts/)(용어수집·데이터추출)
- **SFT 데이터**(파인튜닝 준비, 미실행): [`data/`](data/) finetune_*.jsonl

### 1-B. 답안 해설 생성 + 의학정확성 검증
- **방법**: 정답 알려준 상태로 Claude Opus 에이전트 35개가 517 해설 배치 생성 → **2단계 적대 검증**
  (1차 47에이전트 → 2차 강화 92에이전트, 배치당 독립 리뷰어 2명 union → 독립 확정). 누수 LOO.
- **결과**: 서로 다른 오류 16건(중대4) 교정, **정확도 ≈96.9%**.
- **상세/원자료**: [`dataset/한의학_문제_해설.jsonl`](dataset/), [`dataset/해설_검증_리포트.md`](dataset/해설_검증_리포트.md),
  [`scripts/verify_explanations_*`](scripts/)

### 1-C. GraphRAG + 유형-선택적 RAG (우리 확장, 6모델)
- **방법**: 보기 처방을 지식그래프에서 조회(구성·주치·계통) → 근거 주입. 방식 ①그냥 ②그래프 ③그래프+해설(누수로 제외)
  ④**선택적RAG**(처방형→그래프 / 그외→그냥). Neo4j 불요(Python 재현). **누수방지**: 문항 테스트전용·정답분리·학습없음.
- **결과**: **그래프RAG는 처방형에서만 효과**. qwen 처방형 **+15.1%p**, 선택적RAG 전체 **48.7→51.3%(+2.5%p)**.
  **모델 의존적**: qwen 압도 > gemma2 > exaone/llama/solar 미미 > mistral 역효과(−8.1%p).
- **상세 문서**: [`graphrag/PAPER_한의학_GraphRAG.md`](graphrag/PAPER_한의학_GraphRAG.md)(논문 종합),
  [`graphrag/MULTIMODEL_REPORT.md`](graphrag/MULTIMODEL_REPORT.md)(6모델×방식×과목 표), [`graphrag/SOURCES.md`](graphrag/SOURCES.md)(데이터 출처)
- **원자료/코드**: [`graphrag/eval_out/`](graphrag/eval_out/)(6모델 결과), `graphrag/{build_evidence,run_eval_local,run_all_models,aggregate_models}.py`
- **데이터**: `graphrag/data/`(KG 노드7,692·엣지39,996·처방/용어 청크·문항별 근거+출처)

### 1-D. bigse0u1 원본 GraphRAG (통합)
- **방법(원본)**: Neo4j 지식그래프 + Chroma 벡터DB + **BGE-m3 seed 벡터화**(질문 임베딩↔증상노드 임베딩, LLM번역 제거)
  → 처방 검색. 임상 청크 재작성·누락처방 24개 보완으로 recall 4.7→12.8%.
- **원본 결과**: gpt-4o-mini 처방형 GraphRAG **43.0%**(그냥 30.2%). 로컬: qwen 34.9%·exaone 32.6%·llama 31.4%·mistral 24.4%(역효과).
- **전체 보존**: [`bigse0u1/`](bigse0u1/) — README·step1~4·add_missing_rx·check_clinical_recall·data·eval **전부**
- **복제 하니스**(우리, BGE-m3 동일·그래프 Python 동일로직): [`graphrag/bigse_replicate.py`](graphrag/bigse_replicate.py) *(실행 대기)*

---

## 2. 데이터 출처 (전 실험 공통)
표준한의학용어집(2006) · 한의대 내과학 교과서 처방 데이터(2024) · 팔강변증 한의표준임상진료지침(2023) ·
국시원 공개 기출(한의사 제81회·한약사 제27회, 2026). 저작권 보호 대상, 개인 학습·연구용.

## 3. 누수 방지(전 실험) — 핵심 원칙
- **문항은 테스트에서만** 사용, **정답 분리**(채점 전용), **파인튜닝/학습 없음**(폐쇄형 추론).
- 지식그래프·용어는 교과서/용어집 기반(문항 무관). 해설 기반(정답 사전사용)은 **결과에서 제외**.

## 4. 디렉터리 맵 (main_1)
```
├── EXPERIMENTS.md, HANDOVER.md, README.md, 연구_전체_인덱스.md   # 실험기록·인수인계·인덱스
├── dataset/        # 문항517·해설·검증리포트·용어
├── results/        # 1-A 전 모델 평가 JSON 24개 + report/summary
├── data/           # SFT 데이터(미실행)
├── training/, scripts/   # 평가·RAG·해설검증·데이터추출 코드
├── graphrag/       # 1-C GraphRAG(우리) + 논문문서 + 6모델결과 + bigse 복제 하니스
└── bigse0u1/       # 1-D bigse0u1 원본 GraphRAG 전체(코드·데이터·README)
```

> 논문 작성 시: 방법은 각 실험 문서(1-A~1-D)의 "방법" + 부록(프롬프트·하이퍼파라미터: HANDOVER 14·15장,
> PAPER 부록 A·B·C), 결과는 각 원자료(results/·eval_out/·MULTIMODEL_REPORT)에서 결정론적으로 재계산 가능.
